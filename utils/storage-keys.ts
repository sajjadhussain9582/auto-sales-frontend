/** Central keys for browser storage — change in one place if needed */
export const STORAGE_KEYS = {
  sessionToken: "agentic_session_token",
  recentConversationIds: "agentic_recent_conversations",
} as const;
